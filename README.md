# SP2L + Pro BTB — پیاده‌سازی استراتژی‌های پورصمدی

پیاده‌سازی قابل‌بک‌تست دو استراتژی پرایس‌اکشن محمدعلی پورصمدی —
**SP2L** (اسپایک + دو لگ + سطح) و **Pro BTB** (بازگشت به سطح شکسته‌شده) —
با موتور تشخیص اسپایک مشترک، حالت بک‌تست ترکیبی و اندیکاتورهای تریدینگ‌ویو.

**📘 بلوپرینت‌ها:** [SP2L](docs/BLUEPRINT.md) · [Pro BTB](docs/BTB_BLUEPRINT.md)

**📕 راهنمای کامل کاربری (PDF فارسی):**
[اندیکاتورهای تریدینگ‌ویو](docs/guide_tradingview.pdf) ·
[اکسپرت متاتریدر ۵](docs/guide_mt5.pdf)

## منطق در یک نگاه

```
                     ┌─ SP2L:  پولبک کم‌عمق ──▶ ورود Stop روی شکست B ─ R:R 1:1
اسپایک + گپ قیمتی ──┤
                     └─ BTB:   پولبک عمیق ──▶ ورود Limit روی ریتست سطح شکسته‌شده L ─ R:R 2:1

حد ضرر هر دو: پشت کندل مبدأ اسپایک (نقطه A)
حالت ترکیبی: هر دو ستاپ هم‌زمان، یک پوزیشن در هر لحظه، اولویت با BTB
```

## ساختار پروژه

```
docs/BLUEPRINT.md        بلوپرینت SP2L (فارسی) + منابع
docs/BTB_BLUEPRINT.md    بلوپرینت Pro BTB + فرض‌های پیاده‌سازی + قواعد حالت ترکیبی
docs/guide_tradingview.pdf  راهنمای کامل اندیکاتورها (فارسی، ۱۷ صفحه)
docs/guide_mt5.pdf       راهنمای کامل اکسپرت متاتریدر ۵ (فارسی، ۱۷ صفحه)
docs/build_pdf.py        اسکریپت تولید دوباره PDF از فایل‌های HTML
sp2l/models.py           مدل‌های داده (کندل، کانفیگ، ستاپ، معامله)
sp2l/strategy.py         موتور مشترک تشخیص اسپایک + ماشین حالت SP2L
sp2l/btb.py              ماشین حالت Pro BTB (سطح شکسته‌شده → ریتست → ورود لیمیت)
sp2l/backtest.py         بک‌تستر چندستاپه + آمار کلی و به تفکیک ستاپ
sp2l/data.py             لودر CSV + تولید دیتای مصنوعی برای دمو
sp2l/cli.py              رابط خط فرمان
pine/sp2l_indicator.pine اندیکاتور SP2L تریدینگ‌ویو
pine/btb_indicator.pine  اندیکاتور Pro BTB تریدینگ‌ویو
pine/sp2l_strategy.pine  استراتژی SP2L برای Strategy Tester
mt5/SP2L_Poursamadi_EA.mq5  اکسپرت متاتریدر ۵ (اجرای خودکار SP2L)
mt5/README.md            راهنمای نصب و پارامترهای اکسپرت
tests/                   تست‌های واحد (pytest)
```

## اجرا

بدون هیچ وابستگی خارجی (فقط پایتون ۳.۹+):

```bash
# دمو روی دیتای مصنوعی
python3 -m sp2l --demo

# روی دیتای واقعی خودتان (CSV با ستون‌های time,open,high,low,close[,volume])
python3 -m sp2l XAUUSD_M5.csv

# با پارامترهای سفارشی
python3 -m sp2l XAUUSD_M5.csv --rr 1.5 --min-spike-bars 4 --session-filter --trades

# انتخاب ستاپ: هر دو (پیش‌فرض)، فقط SP2L یا فقط BTB
python3 -m sp2l XAUUSD_M5.csv --setup both
python3 -m sp2l XAUUSD_M5.csv --setup sp2l
python3 -m sp2l XAUUSD_M5.csv --setup btb --btb-rr 2.5 --btb-lookback 12
```

در حالت `both` خروجی، آمار را **به تفکیک ستاپ** هم گزارش می‌کند تا ببینید روی
دیتای شما کدام تاکتیک ورود بهتر جواب می‌دهد.

پارامترهای مهم (پیش‌فرض‌ها مطابق بلوپرینت):

| سوییچ | پیش‌فرض | توضیح |
|---|---|---|
| `--min-spike-bars` | 3 | حداقل کندل متوالی اسپایک |
| `--min-body-ratio` | 0.5 | حداقل نسبت بدنه به رنج کندل |
| `--no-gap` | — | حذف الزام گپ قیمتی (P-Gap/FVG) |
| `--power-mult` | 1.5 | فیلتر قدرت حرکت (× ATR؛ صفر = خاموش) |
| `--rr` | 1.0 | ریسک به ریوارد |
| `--max-wait-bars` | 20 | مهلت تریگر بعد از اسپایک |
| `--scale-in` | — | ورود دوم در ۵۰٪ مسیر ورود تا SL |
| `--session-filter` | — | فقط ورود در سشن (پیش‌فرض 13:30–20:00 UTC) |
| `--risk` | 0.01 | ریسک هر معامله (سهم از اکوییتی) |
| `--setup` | both | ستاپ‌های فعال: `both` / `sp2l` / `btb` |
| `--btb-lookback` | 10 | پنجره یافتن سطح شکسته‌شده قبل از اسپایک |
| `--btb-rr` | 2.0 | ریسک به ریوارد ورودهای BTB |
| `--btb-max-wait-bars` | 30 | مهلت ریتست بعد از اسپایک |
| `--partial` | — | فعال‌سازی پارشال: بستن بخشی از پوزیشن در سطح `--partial-rr` (پیش‌فرض 1R) |
| `--partial-pct` | 0.5 | سهم بسته‌شده در پارشال (۰ تا ۱) |
| `--be` | off | ریسک‌فری: `rr` (رسیدن قیمت به `--be-trigger-rr`) یا `after_partial` (بعد از پارشال) |
| `--be-offset-r` | 0.0 | هنگام ریسک‌فری چند R قفل شود (۰ = دقیقاً نقطه ورود) |
| `--trail-atr` | 0 | تریلینگ استاپ ATR (مثلاً `2` = دنبال کردن قیمت با فاصله 2×ATR؛ فقط سفت می‌شود) |
| `--min-pullback-bars` | 1 | SP2L: حداقل کندل بین شروع پولبک و شکست (اصلاح بالغ‌تر) |
| `--min-retrace` | 0 | SP2L: حداقل عمق پولبک نسبت به اسپایک (مثلاً `0.3` = ۳۰٪) |
| `--cost-r` | 0 | هزینه رفت‌وبرگشت هر معامله بر حسب R (اسپرد + کمیسیون) |
| `--max-trades-day` | 0 | گارد روزانه: سقف تعداد معامله در روز |
| `--max-daily-loss-r` | 0 | گارد روزانه: توقف معامله بعد از X− R ضرر در روز |
| `--allow-concurrent` | — | گرفتن همه سیگنال‌ها حتی وقتی در یک معامله/سفارش باز هستید (اسکیپ غیرفعال) |
| `--break-confirm` | close | شکست B: `close` (کلوز فراتر از B) یا `touch` (سایه یا کلوز) |
| `--entry-mode` | market | ورود SP2L: `market` (اوپن کندل بعدی) یا `limit` (لیمیت روی B) |
| `--limit-wait-bars` | 10 | مهلت پر شدن سفارش لیمیت؛ اگر پر نشد لغو می‌شود و معامله‌ای باز نمی‌شود |
| `--daily` | — | گزارش برایند روزانه (تعداد، برد، سود/زیان، مجموع R هر روز) |

### چهار آپشن ورود

ترکیب `--break-confirm` و `--entry-mode` چهار حالت می‌سازد:

| آپشن | break-confirm | entry-mode | رفتار |
|---|---|---|---|
| ۱ | `touch` | `market` | شکست با سایه یا کلوز → ورود مارکت روی **اوپن کندل بعدی** |
| ۲ | `touch` | `limit` | شکست با سایه یا کلوز → کاشتن **لیمیت روی خط B** |
| ۳ | `close` | `market` | شکست فقط با **کلوز** → ورود مارکت روی اوپن کندل بعدی |
| ۴ | `close` | `limit` | شکست فقط با **کلوز** → کاشتن لیمیت روی خط B |

**هیچ حالتی روی همان کندل شکست پر نمی‌شود.** در حالت `limit`، اگر قیمت تا
`--limit-wait-bars` کندل به B برنگردد، سفارش لغو می‌شود، معامله‌ای ثبت نمی‌شود و
در خروجی به‌عنوان «Limits unfilled» شمرده می‌شود. در ورود مارکت این موضوع
بی‌معناست چون همیشه فعال می‌شود.

دلایل خروج در گزارش معاملات: `target` / `stop` / `be-stop` (استاپ در نقطه
ریسک‌فری) / `trail-stop` (استاپ تریل‌شده در سود) / `eod`.

مثال — پارشال ۵۰٪ در 1R، ریسک‌فری بعد از پارشال، تارگت نهایی 2R و گزارش روزانه:

```bash
python3 -m sp2l XAUUSD_M5.csv --rr 2 --partial --be after_partial --daily --trades
```

## تست‌ها

```bash
python3 -m pytest tests/ -q
```

## تریدینگ‌ویو

سه فایل Pine v5 با منطق یکسان (آینه پیاده‌سازی پایتون) موجود است. برای «حالت
ترکیبی» روی چارت، هر دو اندیکاتور SP2L و BTB را هم‌زمان اضافه کنید.

### اندیکاتور SP2L — `pine/sp2l_indicator.pine`

برای مشاهده زنده ستاپ‌ها روی چارت:

1. در تریدینگ‌ویو پایین صفحه **Pine Editor** را باز کنید.
2. محتوای فایل را کامل پیست کنید و **Add to chart** بزنید.
3. برای آلارم: روی چارت **Alert** بسازید و در قسمت Condition اندیکاتور
   `SP2L` و یکی از رویدادهای `SP2L Long` / `SP2L Short` / `SP2L Any signal`
   را انتخاب کنید.

**گروه Entries & concurrency (فقط SP2L):**

- **Break of B counts on**: `Close only` (کندل باید فراتر از B بسته شود) یا
  `Shadow or close` (لمس سایه هم کافی است).
- **Entry order**: `Market (next bar open)` یا `Limit (at B)` — همان چهار آپشن
  بالا. سفارش در انتظار روی چارت با خط نقطه‌چین/خط‌چین دیده می‌شود و لیمیتِ پر
  نشده با برچسب `◌ Not activated` خاکستری می‌شود.
- **Take all trades**: وقتی روشن باشد، سیگنال‌ها حتی هنگام باز بودن یک معامله
  دیگر گرفته می‌شوند (برچسب `◌ Skipped (in trade)` دیگر ظاهر نمی‌شود) و چند
  معامله هم‌زمان روی چارت مدیریت و رسم می‌شوند. جدول وضعیت تعداد معاملات باز و
  سفارش‌های در انتظار (`+nL`) را نشان می‌دهد.

امکانات نسخه ۲ (شبیه‌سازی کامل معامله روی چارت):

- **باکس اسپایک** با برچسب SPIKE + باکس‌های گپ قیمتی (FVG) و خطوط زنده سطح
  ورود (B، نارنجی) و استاپ (A، قرمز نقطه‌چین) تا وقتی ستاپ فعال است
- **سرنوشت هر ستاپ** روی چارت مشخص است: `✖ Cancelled` (ابطال با برگشت قیمت)،
  `✖ Expired` (انقضای مهلت)، `◌ Skipped` (سیگنال وسط معامله باز)،
  `◌ Off-session` (سیگنال خارج از سشن)
- **خطوط معامله زنده**: Entry (رنگ روند)، SL (قرمز؛ بعد از ریسک‌فری **آبی**
  می‌شود و جابه‌جایی استاپ را نشان می‌دهد)، TP1 پارشال (سبز نقطه‌چین) و TP نهایی
  (سبز) — تا لحظه بسته‌شدن معامله همراه قیمت کشیده می‌شوند
- **پارشال و ریسک‌فری** در تنظیمات: سطح و درصد پارشال، حالت ریسک‌فری
  (`Off` / `At RR trigger` / `After partial`) و آفست قفل سود
- **نتیجه هر معامله** با لیبل بر حسب R: مثلاً `TP +1.50R`، `SL -1.00R`،
  `BE +0.50R`
- **داشبورد** (پایین-راست): برایند هر روز (تعداد معامله، تعداد برد، مجموع R)،
  ردیف جمع کل و لیست آخرین معاملات با ساعت، جهت و نتیجه R
- جدول وضعیت (بالا-راست): فاز ستاپ، جهت، سطوح و وضعیت معامله باز با **R لحظه‌ای**
- پنج آلارم: `Long` / `Short` / `Any signal` / `Partial filled` / `Trade closed`

> شبیه‌سازی معامله در اندیکاتور با همان فرض‌های محافظه‌کارانه بک‌تستر پایتون است
> (برخورد هم‌زمان SL و TP در یک کندل = SL؛ پارشال/ریسک‌فری از کندل بعد از ورود).

### اندیکاتور Pro BTB — `pine/btb_indicator.pine`

همان امکانات نسخه ۲ (شبیه‌سازی معامله، پارشال/ریسک‌فری/تریلینگ، لیبل سرنوشت
ستاپ، داشبورد روزانه و پنج آلارم) با ماشین حالت BTB: سطح شکسته‌شده (L، آبی)
و استاپ (A، قرمز) زنده رسم می‌شوند و ورود در ریتست سطح ثبت می‌شود. لیبل‌های
سرنوشت مخصوص BTB: `✖ Expired` / `✖ Gapped through` (گپ از روی استاپ، بدون
ورود) / `◌ Off-session` / `◌ Skipped` / `◌ Daily limit`.

### فیلترها و گاردهای مشترک هر دو اندیکاتور (گروه Filters & risk guards)

- **ATR trailing stop**: تعقیب قیمت با فاصله n×ATR؛ استاپ فقط سفت می‌شود و
  بعد از جابه‌جایی آبی رنگ می‌شود؛ خروج با برچسب `TRL`
- **Cost per trade (R)**: کسر هزینه اسپرد/کمیسیون از نتیجه هر معامله
- **Daily guard**: سقف تعداد معامله و سقف ضرر روزانه — بعد از فعال‌شدن،
  سیگنال‌های همان روز با لیبل `◌ Daily limit` رد می‌شوند

### شخصی‌سازی نمایش (هر دو اندیکاتور)

- **گروه Trade lines**: نوع خط (Solid/Dashed/Dotted)، ضخامت (۱ تا ۵)، طول
  اضافی به سمت راست (Extend lines right)، رنگ مجزای خطوط ورود/استاپ/تارگت، و
  یک سوییچ برای نمایش/عدم‌نمایش قیمت‌های E/SL/TP داخل باکس سیگنال (برای وقتی
  چارت شلوغ می‌شود).
- **گروه Risk / reward zones**: نمایش محدوده ریسک (قرمز، از ورود تا استاپ) و
  محدوده ریوارد (سبز، از ورود تا تارگت) درست مثل ابزار Long/Short Position
  تریدینگ‌ویو، به‌همراه برچسب نسبت `R:R 1:x`؛ رنگ هر دو محدوده قابل‌تنظیم.
- **گروه Box transparency**: تنظیم شفافیت جداگانه‌ی فیل و بوردر باکس اسپایک
  (و FVG در SP2L) — مثلاً بوردر را کم‌رنگ‌تر کنید تا شدو/ویک کندل‌ها بهتر دیده
  شوند.
- **گروه History**: با `Limit chart drawings to recent days`، فقط رسم‌های
  N روز اخیر روی چارت نشان داده می‌شوند (کنترل شلوغی و سقف ۵۰۰ آبجکت Pine)؛
  داشبورد و آمار همچنان کل تاریخچه را می‌شمارند.
- **سازگاری با Bar Replay**: چون گیت History بر پایه `last_bar_time` است که در
  حالت ریپلی روی نشانگر ریپلی تنظیم می‌شود، اندیکاتور هنگام استفاده از ابزار
  ریپلی هم درست کار می‌کند و رسم‌ها را بار به بار نشان می‌دهد.

### استراتژی SP2L — `pine/sp2l_strategy.pine`

برای بک‌تست در **Strategy Tester** خود تریدینگ‌ویو. هنگام شروع پولبک، سفارش
Stop روی سطح B پارک می‌شود تا مثل بک‌تستر پایتون داخل کندل روی سطح فیل شود؛
با ابطال یا انقضای ستاپ، سفارش لغو می‌شود. حجم بر اساس درصد ریسک از اکوییتی
محاسبه می‌شود (برای نمادهای CFD/اسپات دقیق است؛ برای فیوچرز مقیاس قرارداد را
خودتان تنظیم کنید).

## متاتریدر ۵ (اجرای خودکار)

اکسپرت `mt5/SP2L_Poursamadi_EA.mq5` همان منطق SP2L را روی متاتریدر ۵ اجرا می‌کند:
هر چهار آپشن ورود، محاسبه خودکار حجم از درصد ریسک، پارشال، ریسک‌فری، تریلینگ
ATR، گاردهای روزانه، فیلتر سشن و پنل وضعیت روی چارت. همه تصمیم‌ها روی
**کندل بسته‌شده** گرفته می‌شوند تا رفتارش با بک‌تست پایتون هم‌خوان باشد.

راهنمای کامل نصب، پارامترها و بک‌تست: [`mt5/README.md`](mt5/README.md)

## ⚠️ سلب مسئولیت

- خروجی دموی `--demo` روی **دیتای مصنوعی** است و هیچ معنایی درباره بازار واقعی ندارد؛
  حتماً روی دیتای واقعی نماد و تایم‌فریم خودتان بک‌تست بگیرید.
- این مخزن صرفاً آموزشی/پژوهشی است و توصیه سرمایه‌گذاری نیست. معامله با اهرم
  می‌تواند به از دست رفتن کل سرمایه منجر شود.
